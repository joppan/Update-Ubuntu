#!/bin/bash

echo "The system will update now. Please provide your admin password whenever prompted."

sudo apt-get update && sudo apt-get dist-upgrade && sudo apt-get autoremove && sudo apt-get autoclean
